syntax = "proto3";

package discomblebot;

message BotMessage {

  enum Type {
    STATUS = 0;
    INVITE = 1;
    QUIT = 2;
  }
 
  enum Direction {
    REQUEST = 0;
    RESPONSE = 1;
  }

  Type type = 1;
  Direction direction = 2;

}
